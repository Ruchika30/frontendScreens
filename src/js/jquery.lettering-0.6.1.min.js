/* Lettering.JS 0.6.1 by Dave Rupert  - http://daverupert.com */
(function ($) { function injector(t, splitter, klass, after) { const a = t.text().split(splitter); let inject = ''; if (a.length) { $(a).each((i, item) => { inject += `<span class="${klass}${i + 1}">${item}</span>${after}`; }); t.empty().append(inject); } } const methods = { init() { return this.each(function () { injector($(this), '', 'char', ''); }); }, words() { return this.each(function () { injector($(this), ' ', 'word', ' '); }); }, lines() { return this.each(function () { const r = 'eefec303079ad17405c889e092e105b0'; injector($(this).children('br').replaceWith(r).end(), r, 'line', ''); }); } }; $.fn.lettering = function (method) { if (method && methods[method]) { return methods[method].apply(this, [].slice.call(arguments, 1)); } if (method === 'letters' || !method) { return methods.init.apply(this, [].slice.call(arguments, 0)); }$.error(`Method ${method} does not exist on jQuery.lettering`); return this; }; }(jQuery));
